# Backend example env
APP_USERNAME=admin
APP_PASSWORD=password
PORT=3001
# The origin of your frontend (set to your deployed frontend URL in production)
FRONTEND_ORIGIN=http://localhost:5173

# Optional: for deployments where local OAuth isn't possible, set GOOGLE_TOKEN to the JSON
# produced by `node generate-token.js` (copy the printed JSON as one line)
# GOOGLE_TOKEN={...}
